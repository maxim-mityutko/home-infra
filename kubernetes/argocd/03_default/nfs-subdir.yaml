apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nfs-subdir
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  destination:
    name: in-cluster
    namespace: kube-system
  source:
    path: kubernetes/cluster/default/nfs-subdir
    repoURL: 'https://github.com/maxim-mityutko/home-infra.git'
    targetRevision: main
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true