apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: profilarr
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "16"
  labels:
    group: media
spec:
  destination:
    name: in-cluster
    namespace: media
  source:
    path: kubernetes/cluster/media/profilarr
    repoURL: 'https://github.com/maxim-mityutko/home-infra.git'
    targetRevision: main
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
