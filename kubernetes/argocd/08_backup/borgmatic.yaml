apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: borgmatic
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "13"
spec:
  destination:
    name: in-cluster
    namespace: backup
  source:
    path: kubernetes/cluster/backup/borgmatic
    repoURL: 'https://github.com/maxim-mityutko/home-infra.git'
    targetRevision: main
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
