apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: browserless
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  destination:
    name: ''
    namespace: ai
    name: in-cluster
  source:
    path: kubernetes/cluster/ai/browserless
    repoURL: 'https://github.com/maxim-mityutko/home-infra.git'
    targetRevision: main
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
