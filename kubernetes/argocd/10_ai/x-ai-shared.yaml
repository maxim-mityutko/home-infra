apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: x-ai-shared
  namespace: argocd
spec:
  destination:
    name: in-cluster
    namespace: ai
  source:
    path: kubernetes/cluster/ai/x-ai-shared
    repoURL: 'https://github.com/maxim-mityutko/home-infra.git'
    targetRevision: main
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
