---
# NOTE: This is a placeholder secret for the PR.
# You will need to either:
# 1. Encrypt the real values using kubeseal or your SealedSecrets controller
# 2. Replace with sealed secret content after creation
apiVersion: v1
kind: Secret
metadata:
  name: codex
  namespace: ai
  labels:
    app.kubernetes.io/name: codex
type: Opaque
stringData:
  CODEX_API_KEY: "REPLACE_WITH_REAL_KEY"
  CODEX_APP_SERVER_TOKEN: "REPLACE_WITH_REAL_TOKEN"