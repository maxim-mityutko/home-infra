---
apiVersion: http.keda.sh/v1beta1
kind: InterceptorRoute
metadata:
  name: vm-mcp-scaler
spec:
  target:
    service: vm-mcp
    port: 8080
  rules:
    - hosts:
        - mcp.vm.brhd.io
  scalingMetric:
    concurrency:
      targetValue: 25
  # timeouts:
  #   readiness: 30s
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: vm-mcp-scaler
spec:
  scaleTargetRef:
    name: vm-mcp
  minReplicaCount: 0
  maxReplicaCount: 1
  pollingInterval: 60
  initialCooldownPeriod: 600
  cooldownPeriod: 1800
  triggers:
    - type: external-push
      metadata:
        scalerAddress: keda-add-ons-http-external-scaler.keda:9090
        interceptorRoute: vm-mcp-scaler
