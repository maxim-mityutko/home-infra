---
# Borgmatic -- /root/.config/borg
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: borg-keyfiles
  labels:
    app.kubernetes.io/name: borgmatic-config
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: nfs-subdir
  resources:
    requests:
      storage: 1M