# Ref: https://microk8s.io/docs/change-cidr#configure-calico
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system
resources:
  # Sourced from (either):
  #   - Use the version that matches the cluster: 
  #     https://github.com/canonical/microk8s/blob/v1.32/upgrade-scripts/000-switch-to-calico/resources/calico.yaml
  #   - /snap/microk8s/current/upgrade-scripts/000-switch-to-calico/resources/calico.yaml
  - calico.yaml
  - cron-job.yaml
patches:
  - path: patch.yaml
