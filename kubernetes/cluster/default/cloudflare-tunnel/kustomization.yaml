apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cloudflared.yaml
  - secret.yaml
configMapGenerator:
  - name: cloudflared
    files:
      - config/tunnel.yaml
    options:
      disableNameSuffixHash: true
