apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: gpu
configMapGenerator:
  - name: intel-gpu-metrics-dashboard
    files:
      # Based on: https://grafana.com/grafana/dashboards/23251-intel-gpu-metrics/
      - intel-gpu-metrics.json 
generatorOptions:
  labels:
    grafana-dashboard: "true"