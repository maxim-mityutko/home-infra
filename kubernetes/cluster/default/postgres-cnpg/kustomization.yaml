apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - postgres-cnpg.yaml
  - storage-class.yaml
  - secret.yaml
# TODO: create external service for "postgres-cnpg-rw"
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: postgres-external
#   namespace: default
#   labels:
#     app.kubernetes.io/name: postgres
# spec:
#   type: NodePort # 30000-32767
#   ports:
#     - name: postgres
#       port: 30432
#       targetPort: 5432
#       nodePort: 30432
#       protocol: TCP
#   selector:
#     app.kubernetes.io/name: postgres