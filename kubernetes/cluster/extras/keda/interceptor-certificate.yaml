---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keda-http-interceptor
  namespace: keda
spec:
  secretName: keda-http-interceptor-tls
  issuerRef:
    name: lets-encrypt
    kind: ClusterIssuer
  commonName: keda.brhd.io
  dnsNames:
    - keda.brhd.io
