---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mazanoke
  namespace: extras
  labels:
    app.kubernetes.io/name: mazanoke
  annotations:
    cert-manager.io/cluster-issuer: lets-encrypt
spec:
  ingressClassName: public
  rules:
    - host: image.brhd.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: keda-http-interceptor
                port:
                  number: 8080
  tls:
    - hosts:
        - image.brhd.io
      secretName: image-brhd-io-tls
---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: image.brhd.io
  namespace: extras
spec:
  endpoints:
    - dnsName: image.brhd.io
      recordTTL: 86400
      recordType: CNAME
      targets:
        - casa.brhd.io
