apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: registry.brhd.io
  namespace: extras
spec:
  endpoints:
    - dnsName: registry.brhd.io
      recordTTL: 86400
      recordType: CNAME
      targets:
        - casa.brhd.io
        - forge.brhd.io
