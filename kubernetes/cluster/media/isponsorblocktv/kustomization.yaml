apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: media
resources:
  - isponsorblocktv.yaml
configMapGenerator:
  - name: isponsorblocktv-config-json
    files:
    - config/config.json
