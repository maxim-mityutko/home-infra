apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - qbitmanage.yaml
  - volumes.yaml
configMapGenerator:
  - name: qbitmanage-config-yml
    files:
    - config/config.yml
# generatorOptions:
#   disableNameSuffixHash: true
