apiVersion: apps/v1
kind: Deployment
metadata: 
  name: immich-server
spec:
  template:
    spec:
      securityContext:
        supplementalGroups:
          - 993
      containers:
        - name: immich-server
          securityContext:
            privileged: true
          volumeMounts:
            - name: render-device
              mountPath: /dev/dri/renderD128
          env:
            - name: DB_DATABASE_NAME
              valueFrom:
                secretRef:
                  name: immich
                  key: DB_DATABASE_NAME
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: immich
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretRef:
                  name: immich
                  key: DB_PASSWORD
      volumes:
        - name: render-device
          hostPath:
            path: /dev/dri/renderD128
